language: python
env:
  - APP=birmingham_cabinet
  - APP=redundancy_payments_alpha
python:
  - "2.7"
# command to install dependencies
install:
  - pip install -r $APP/requirements.dev.txt --use-mirrors
# command to run tests
script:
  - cd $APP; ./run_tests.sh
notifications:
  email: false
